export const originPath = () => {
    return "https://emtheskills.github.io/software-engineer-resume/"
    // if (process.env.STAGE_ENV !== "PROD") {
    //     return window.location.origin
    // } else {
    //    return window.location.href
    // }
}